<section>
    <div class="container mt-5">
    <div class="row">
        {{#each products}}
        <div class="col-md-3">
           <div class="card" style="width: 15rem; height:29rem">
  <img src="/Public/product-images/{{this.product_id}}"style="width:50px ; height: 50px;" class="card-img-top" alt="...">
  <div class="card-body">
    <h4 class="card-title">{{this.name}}</h4>
    <p class="card-text">{{this.Description}}</p>
    <a href="#" class="btn btn-primary">Add to Cart</a>
  </div>
</div> 
        </div>
         {{/each}}
    </div>
    </div>
   </section>